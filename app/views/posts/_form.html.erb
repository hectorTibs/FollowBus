<%= form_for(post) do |f| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reporteid %>
    <%= f.number_field :reporteid %>
  </div>

  <div class="field">
    <%= f.label :comentario %>
    <%= f.text_field :comentario %>
  </div>

  <div class="field">
    <%= f.label :redsocial %>
    <%= f.text_field :redsocial %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
